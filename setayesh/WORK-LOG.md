# یادداشتِ کارها و مشکلات — ستایش

> قبل از هر تحویلِ کد، این فایل و `OWNER-REQUESTS.md` را بخوان. هر مشکلی که
> درست شد و هر کاری که انجام شد اینجا ثبت می‌شود، نسخه‌به‌نسخه. هدف: هیچ خواسته‌ای
> فراموش نشود و هیچ فیکسی دوباره خراب نشود.

## چطور کار می‌کنم (هر بار)
1. اول `OWNER-REQUESTS.md` را بخوان (دستورهای ثابتِ جاوید).
2. این `WORK-LOG.md` را بخوان (چه چیزهایی قبلاً درست شده — دوباره خرابش نکن).
3. کار را انجام بده، `node -c` + `npm test` (۱۰۰٪ سبز، چند بار).
4. نسخه را در ۵ نشانه هماهنگ کن، این فایل را به‌روزرسانی کن، commit + push.
5. زیپِ کاملِ تمیز بساز و تحویل بده.

---

## نسخه‌ها و فیکس‌ها (این جلسه: ۹.۹.۱۱۹ → اکنون)

### 9.9.120 — انگلیسیِ کامل (اسکلت و پنل‌ها)
- سوییپِ زبان: پنل‌هایی که بعد از سوییچِ زبان ساخته می‌شوند (شیت‌های کلون‌شده)
  حالا دوباره ترجمه می‌شوند (`window.__applyDataI18n` که سوییپ صدا می‌زند).
- `window.i18nLookup` برای ترجمهٔ رشته‌های داینامیکِ سرور.
- محافظِ ضدِ درهم‌رفتگی + دیکشنریِ کامل‌تر؛ پنل‌های مرکز کنترل، تأییدها، ایمیل/تلگرام.

### 9.9.121 — حافظهٔ تلگرام + گراندینگِ مرتبط
- تلگرام حافظهٔ گفتگو گرفت (۸ پیامِ آخر) تا سؤالِ پیرو بی‌بافت نماند.
- پرامپتِ تلگرام سبک شد (دامپِ کاملِ دانش/والت حذف)؛ فقط گراندینگِ مرتبط.

### 9.9.122 — گاردِ دامپِ مغز + انگلیسیِ گوشی
- وقتی همهٔ موتورهای ابری ۴۲۹ بودند و می‌افتاد روی مغزِ پایتون، دامپِ
  «بر اساس دانش والت — X» دیگر به‌عنوان جواب پذیرفته نمی‌شود؛ پیامِ صادقانهٔ
  «موتورها مشغول‌اند» می‌آید.
- ۱۵ پاراگرافِ توضیحیِ فارسی (تابلو/موتورها/آپدیت/والت/گیت‌هاب/گوگل...) ترجمه شد.

### 9.9.123 — پایهٔ انگلیسیِ پنل‌های داینامیک
- ۱۱۱ رشتهٔ ثابتِ فارسی در `app.js` یک‌جا زبان‌آگاه شد (اسکریپتِ امن).
- دستی: پنل «وضعیت حساب»، کارت‌های تابلوی خانواده، تأییدها، وضعیت ایمیل،
  اینباکس، چیپ‌های آنلاین/آفلاین، خط‌های آپدیت، وضعیت مغز پایتون، انتخابگر موتور.
- تأیید در مرورگر: هر ۱۰ زیرتبِ مرکز کنترل و منوی گوشی صفر فارسی (جز محتوای کاربر/لاگ).

### 9.9.124 — «همیشه سقف پر» + خوداموزی + ماژول
- **باگ ۴۲۹:** خطای محدودیتِ نرخ مثل موتورِ خراب رفتار می‌کرد → ۳ بار = قرنطینهٔ
  ۶ ساعته. حالا ۴۲۹ فقط ~۲۰ ثانیه استراحت، هرگز قرنطینه، به streak اضافه نمی‌شود.
- **خوداموزی:** اگر تنها موتورِ سالم Gemini باشد، تحقیق رویش اجرا می‌شود (قبلاً
  Gemini کنار گذاشته می‌شد و تحقیق متوقف می‌شد).
- ماژولِ خالصِ `engineselect.js` (classifyQuestion + cooldownFor) + تست.

### 9.9.125 — ماژولِ providererror.js
- `friendlyProviderError` به ماژولِ خالصِ جدا رفت + تست.

### 9.9.126 — Gemini/مدلِ چت/لو رفتنِ ابزار + نظافت
- **Gemini وصل نمی‌شد:** مدلِ پیش‌فرض نامِ جعلی `gemini-3.6-flash` بود → ۴۰۴.
  حالا پیش‌فرضِ واقعی `gemini-2.0-flash` و لیستِ ترجیح فقط مدلِ واقعی.
- **مدلِ کد برای سؤالِ عادی:** سؤالِ آب‌وهوا با codestral جواب می‌شد. حالا اگر
  مدلِ انتخابیِ کلاینت مدلِ کد باشد ولی سؤال کد نباشد → مدلِ عمومی.
- **لو رفتنِ اسمِ ابزار:** جوابی که فقط `web_fetch` است حذف می‌شود.
- **rollback:** فقط ۲۰ نسخهٔ آخر نگه داشته می‌شود (فایلِ مرده جمع نشود).

### 9.9.127 — ثبتِ دستورهای صاحب
- `OWNER-REQUESTS.md` ساخته شد؛ `CLAUDE.md` به آن اشاره می‌کند.

### 9.9.128 — یادداشتِ کارها
- `WORK-LOG.md` ساخته شد؛ قبل از هر تحویل خوانده می‌شود.

### 9.9.129 — پروفایلِ صاحب برای مغزِ ستایش + خوداموزی
- `pybrain/vault/knowledge/owner-profile.md` ساخته شد تا خودِ مغزِ ستایش هم
  ترجیحاتِ جاوید را بداند (زبان، لحن، علاقه به کد/اپ، جوابِ کوتاه و صادقانه).
  این نوت را `brainVaultBlock()` و موتورِ جست‌وجوی داخلی (`insight`) می‌خوانند.
- تأیید شد که خوداموزی/تحقیق پیش‌فرض روشن است (خط ~۴۶۷۱ و ۴۶۷۸؛ کیل‌سوئیچ
  `ENABLE_RESEARCH=0`). باگِ «موتوری آماده نیست» در ۹.۹.۱۲۴ حل شده بود.

---

## مشکلاتِ باز / در انتظارِ تأییدِ جاوید
- **Gemini روی دستگاهِ جاوید:** بعد از نصبِ ۹.۹.۱۲۶+، اگر باز وصل نشد، احتمالاً
  کلید رد می‌شود (۴۰۱) یا سهمیه پر است (۴۲۹) — نیاز به عکسِ تبِ Engines.
- **کاربر روی نسخهٔ قدیمی می‌ماند:** بارها فیکس فرستادم ولی جاوید هنوز نسخهٔ
  قبلی را اجرا می‌کرد؛ همیشه یادآوری کن «اول نصب کن».
- **ماژوله‌کردنِ `index.js`** ادامه دارد (هنوز ~۹٬۲۶۰ خط).

---
_آخرین به‌روزرسانی: با هر تحویل._
